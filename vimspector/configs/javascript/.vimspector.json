{
  "configurations": {
    "run": {
      "adapter": "vscode-node",
      "configuration": {
        "request": "launch",
        "protocol": "auto",
        "port": "${NODE_DEBUG_PORT}",
        "stopOnEntry": true,
        "console": "integratedTerminal",
        "cwd": "${workspaceRoot}"
      }
    }
  }
}
